# GameSave Manager TODO

本文档只保留尚未完成或仍需验证的事项。已经实现的能力以 [README.md](README.md) 为准。

## P0：数据安全与同步验证

- [ ] 使用真实游戏存档验证空目录、嵌套目录和大量小文件扫描。
- [ ] Hash 计算期间修改文件，确认 Manifest 构建安全失败，不生成半一致快照。
- [ ] 验证 Reparse Point、符号链接和目录循环不会被递归遍历。
- [ ] 验证相同内容对应多个文件路径时只上传一个缺失内容对象。
- [ ] 验证重复提交相同 Manifest 时服务端返回幂等结果，不创建重复快照。
- [ ] 验证首次同步远端 HEAD 为空时可以正常创建快照。
- [ ] 验证本地 HEAD 与远端 HEAD 不一致时普通同步始终被阻断。
- [ ] 验证服务端返回 `409 SYNC_CONFLICT` 时客户端保留稳定错误码并展示明确操作入口。
- [ ] 完成两台真实设备并发修改、冲突提交、恢复云端版本和保留本机版本测试。
- [ ] 完成客户端、CMS、数据库和 MinIO 的完整 Docker Compose 冒烟链路。

## P0：安全恢复验证

- [ ] 分别在对象下载、Hash 校验、staging 构建、目录切换和最终校验阶段模拟进程退出。
- [ ] 验证应用重启后只在确定安全的状态自动回滚，其他现场不会被静默覆盖。
- [ ] 验证恢复成功后原存档安全备份仍然保留。
- [ ] 验证下载对象大小或 SHA-256 不匹配时恢复立即终止。
- [ ] 验证预签名地址过期、网络中断和磁盘空间不足时的错误提示与重试行为。
- [ ] 验证设备 Token 只发送给 GameSave 服务端，不会出现在对象存储请求中。

## P1：本地数据与凭据验证

- [ ] 重启客户端后验证 SQLite Hash Cache 可以稳定命中。
- [ ] 验证旧版本数据库升级、事务回滚和未来版本数据库保护。
- [ ] 验证 SQLite 和日志中不存在设备 Token、密码或 Authorization 明文。
- [ ] 验证设备 Token 存储于 Windows Credential Manager，并按规范化服务端地址隔离。
- [ ] 验证撤销设备后旧 Token 立即失效，当前设备保护逻辑不会误撤销当前会话。
- [ ] 验证不同服务端、不同账号和相同 `gameId` 之间的本地配置不会串用。

## P1：自动同步与游戏发现

- [ ] 验证多游戏同时监控时不会重复执行或并发覆盖同一游戏同步任务。
- [ ] 验证游戏异常退出、进程名重复和进程状态轮询校准。
- [ ] 扩充常见游戏存档路径规则，并保留用户手工覆盖优先级。
- [ ] 验证 Steam 多库、Epic 清单异常和 GOG 注册表缺失场景。
- [ ] 验证开机启动启用、关闭、程序移动和卸载后的注册项清理。

## P1：测试与工程质量

- [ ] 为 Domain 和 Application 核心规则增加单元测试。
- [ ] 为 SQLite、Windows Credential Manager、文件系统和进程监控增加 Windows 集成测试。
- [ ] 为同步、恢复、导出和保留策略建立可重复执行的端到端测试。
- [ ] 评估使用 CommunityToolkit.Mvvm 减少 ViewModel 样板代码，避免在功能开发期间一次性大规模重构。
- [ ] 增加关键错误码、恢复阶段和同步阶段的结构化诊断信息。
- [ ] 建立数据库迁移兼容性测试样本，覆盖所有已发布 schema 版本。

## P1：发布基础设施

- [ ] 在受保护的 CI 凭据库中配置代码签名证书和时间戳服务。
- [ ] 使用 `signtool` 对客户端 EXE 和安装包签名，并验证 Windows SmartScreen 表现。
- [ ] 建立 HTTPS 发布源、已签名版本清单和失败回滚策略。
- [ ] 实现自动更新前的完整性校验、签名校验和版本降级保护。
- [ ] 在真实 ARM64 Windows 设备上验证 `win-arm64` 发布物。
- [ ] 验证安装、覆盖升级、卸载和用户数据保留行为。
- [ ] 根据真实发布物评估 trimming，禁止仅为了减小体积破坏 WPF、SQLite 或系统凭据调用。

## P2：产品完善

- [ ] 增加首次使用引导，明确服务端地址、存档目录和自动同步的安全边界。
- [ ] 增加冲突版本差异摘要，至少展示文件数量、总大小和修改时间差异。
- [ ] 增加恢复前磁盘空间检查和目标目录写权限检查。
- [ ] 增加日志导出与问题诊断包，导出前必须再次执行敏感字段脱敏。
- [ ] 补充最新客户端截图和正式发布说明。

## 完成标准

任务完成后应同时满足：

1. 对应代码或基础设施已实现。
2. 自动化测试或可复现的人工验证步骤已补充。
3. README 中受影响的行为、限制或命令已同步更新。
4. 不引入明文凭据、静默覆盖数据或不可回滚的数据迁移。
