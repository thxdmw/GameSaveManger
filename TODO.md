# 后续任务清单

> 已完成：修复同步进度条的只读属性绑定，应用可正常启动。

## P0：发布前必须完成

- [x] 启动冒烟测试：已在独立 STA UI 线程中加载应用资源、创建主窗口及全部页面，并将 WPF 绑定错误视为失败；不依赖 Docker 或后端服务。
- [ ] 存档安全回归：覆盖“同步成功、同步取消、网络中断、磁盘空间不足、恢复中断”六类场景，确认原始存档不会被静默覆盖。
- [x] 故障日志：启动失败或同步失败时，将完整异常、内部异常和调用栈写入本地日志；界面仅展示易读的错误说明。
- [ ] 请求 ID：待确认已部署服务端返回请求 ID 的响应头或字段后，再将其关联到客户端错误日志。
- [ ] 发布包验收：用安装包安装、卸载、升级一次，确认配置、凭据和本地数据库的保留或清理行为符合预期。

## P1：同步可靠性

- [ ] 为只读 ViewModel 属性的显示绑定统一显式使用 `Mode=OneWay`，并增加检查以避免同类启动错误。
- [ ] 为登录失效、超时、服务端 5xx、限流和断网设计统一的提示、退避重试与“立即重试”入口。
- [x] 同步结果增加摘要：展示上传对象数、文件数、数据量、耗时及冲突后的下一步操作。下载失败项仍由恢复流程单独提示。
- [ ] 补充多设备冲突的界面流程与测试：清楚展示本地版本、远端版本及“恢复远端/保留本地”的后果。

## P2：自动化验证与维护

- [x] 增加 WPF 页面加载测试，防止 XAML 绑定错误只能在运行时发现。
- [x] 将现有基础验证接入持续集成，在提交和发布前自动执行构建与验证。
- [x] 统一项目中文文本与资源文件为 UTF-8；修复维护时出现的乱码，并在编辑器配置中固定编码。
- [ ] 为本地 SQLite 数据库迁移、回滚与损坏恢复建立备份和验证用例。

## P3：产品体验

- [ ] 完善游戏发现与存档目录建议：允许用户确认、编辑并保存每个游戏的目录规则。
- [ ] 增加同步历史与健康状态页面，便于查看最近一次成功同步、待同步内容和失败原因。
- [ ] 评估应用自动更新与安装包签名，降低发布后的升级和安全风险。
## 游戏发现与存档识别优化（实施计划）

### 第一批：最小可用版本

- [x] 增加 `GameIdentity`、`SaveLocationCandidate` 与统一检测接口。
- [x] 支持从本地 EXE 生成 `LOCAL` 游戏身份。
- [x] 扫描安装目录常见存档目录、常用 Windows 用户目录和 Steam userdata。
- [x] 候选结果显示来源、可信度、原因、文件数量、大小与样本文件。
- [x] 检测结果不再自动采用；确认前禁止同步和自动同步。
- [x] 首次确认时展示文件数量和逻辑大小预览，并保存确认状态。

### 第二批：提高识别准确率

- [x] 内置 Ludusavi Manifest 与许可证说明入口，离线按 Steam/GOG ID 或规范化名称匹配。
- [x] 建立进程内 Manifest 索引，避免每次检测重复解析 YAML。
- [x] 使用 EXE 评分替代按文件名的首个 EXE 选择；排除卸载、安装与崩溃报告程序。
- [x] 支持 Steam AppID 对应 userdata 候选目录。
- [x] 对缓存、日志、截图、崩溃报告和安装组件目录降权或排除。

### 第三批：覆盖未知和特殊游戏

- [x] 支持“启动游戏并学习”：运行前后只比较文件元数据，不计算 Hash、不上传文件。
- [x] 将变化文件按目录聚合为可解释候选，仍需用户确认。
- [x] 在同步页提供候选解释与确认入口。
- [x] 将本地游戏 EXE 选择、平台扫描和存档检测收敛到添加流程。
- [ ] 完成独立的分步游戏添加向导与游戏卡片健康状态展示。

> 当前为首次开发 schema：旧开发期 `local_game_profile` 表会重建，不迁移旧 SQLite 配置。

### 第四批：支持复杂存档结构

- [x] 本地配置支持多个已确认的 `SaveRootRule`，以 JSON 保存根目录、来源、可信度和包含/排除规则。
- [x] 扫描、Manifest 和上传统一应用 Include/Exclude Glob；拒绝绝对路径与 `..` 路径穿越规则。
- [x] 多根目录文件以 `rootId/相对路径` 写入 Manifest，避免同名文件冲突，并保持 CMS 快照接口兼容。
- [x] 每个根目录独立扫描，跳过重解析点，且只有全部确认后才允许同步；同步中心可添加、移除并持久化附加目录。
- [x] CMS GameSave 服务端通过现有相对路径校验兼容多根目录命名空间；已补充协议说明并完成 Maven 编译。
- [x] 注册表存档：仅允许用户明确配置的 HKCU 键；导出为独立 JSON 快照文件，恢复前自动保留本机注册表安全备份并校验规则路径。
- [x] Manifest 在线更新：设置页显示本地版本并支持手动检查；使用 ETag、SHA-256 元数据和临时文件原子替换，失败时保留内置/上一份可用规则。
## 核心恢复安全（持续实施）

- [x] 多根目录恢复：每个破坏性操作前后均持久化 Journal，启动恢复按磁盘事实回滚并标记 RolledBack。
- [x] 注册表恢复纳入同一文件恢复事务：准备阶段校验快照并导出安全副本；文件成功后才应用；失败或启动恢复时回滚注册表。
- [ ] 补齐注册表与文件恢复交叉失败的故障注入覆盖。
- [ ] 扩展恢复中断回归到文件下载、文件应用、注册表应用和人工介入场景。
