# Draft PR summary

Introduces the .NET 10 WPF V2 client beside the legacy WinForms application. The first vertical slice builds immutable save manifests from an authoritative full directory scan and canonical SHA-256 content identities.
